version: "3"
services:
    db:
        image: postgres
        volumes:
            - psql:/var/lib/postgresql/data

        ports:
            - 5432:5432

        environment:
            - POSTGRES_DB=postgress
            - POSTGRES_USER=postgres
            - POSTGRES_PASSWORD=postgres
volumes:
    psql:
